#!/usr/bin/env bash

set -euo pipefail

SCRIPT_ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)
WORKTREE_ROOT=$(cd "${T3CODE_WORKTREE_PATH:-$SCRIPT_ROOT}" && pwd -P)

BEA_ENV_ROOT="$WORKTREE_ROOT" "$WORKTREE_ROOT/scripts/bea-env" up "$@"
