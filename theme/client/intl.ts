import { integrateIntl } from '@/theme-kit/client';

export const {
    useIntl,
    useLocaleSlug,
    IntlContextProvider,
    FormattedMessage,
    FormattedDate,
    FormattedTime,
} = integrateIntl();
